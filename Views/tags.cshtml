@{
  Layout="Shared/_Layout.cshtml";
}

<h2>Tags:</h2>
@if (Model.Count != 0)
{
  <ul>
    @foreach (var tag in Model)
    {
      <li><a href="/tags/@tag.GetId()">@tag.GetName()</a></li>
    }
  </ul>
}
<h4><a href="/tags/new">Add a new tag</a></h4>
<h4><a href="/">Return home</a></h4>
